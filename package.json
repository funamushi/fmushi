{
  "name": "fmushi",
  "version": "0.0.0",
  "description": "F mushi",
  "main": "server.coffee",
  "engines": {
    "node": "0.10.26"
  },
  "scripts": {
    "test": "NODE_ENV=test mocha test/*.test.coffee && karma start karma.coffee --reporters dots --browsers PhantomJS --single-run",
    "migrate": "sequelize -m --coffee"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:funamushi/fmushi.git"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
